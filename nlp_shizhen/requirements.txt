# Dependencies for the NLP container.

# fastapi / uvicorn are left unpinned: vllm transitively requires a recent
# starlette (>=0.49.1), which an old pinned fastapi would conflict with.
fastapi
uvicorn[standard]
# vllm pins its own torch build; it powers fast batched generation.
vllm==0.21.0
ninja
transformers
sentence-transformers
accelerate
numpy
tiktoken
sentencepiece
rank-bm25
