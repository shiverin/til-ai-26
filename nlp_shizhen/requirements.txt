# Dependencies for the NLP cheese container.
#
# Cheese mode answers every question with a pre-computed adversarial trigger,
# so there is NO LLM generation: vllm / ninja / tiktoken / Phi-4-mini are not
# needed. Only the retrieval stack (embedder + reranker + BM25) is required so
# the document-overlap gate is satisfied genuinely.
fastapi
uvicorn[standard]
transformers
sentence-transformers
numpy
sentencepiece
rank-bm25
